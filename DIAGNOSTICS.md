# 接続診断（0.4.2-diagnostics.1）

これは原因を切り分ける診断版です。再接続先の選択・Bluetooth修復・インジケータの配置は変更していません。
仮想出力を入力元に選ぶ可能性、接続済み機器があると再選択しない挙動、ハンドルを開いただけで接続表示する挙動は残っています。

## 記録の取り方

1. 旧版をタスクトレイから終了し、診断版を1つだけ起動します。設定のデバッグモードをオンにします。
2. 出力方式と入力元の選択（自動かHVC指定か）を記録します。途中で複数の条件を変えないでください。
3. HVCのボタン操作、スリープ／切断、復帰操作、再接続ボタンを押した時刻をメモします。
4. 設定の「ログフォルダー」から該当日のログを取得します。日付をまたいだ場合は両日分が必要です。
5. 比較は各条件につきまず3回、傾向が出たら10回まで確認します。各試行の途中で再ペアリングしません。
   復帰不能なら失敗として記録し、次の条件の開始前に基準状態を揃えます。再ペアリングした時刻も残します。

ログには機器名、HIDパス、Bluetoothアドレスが含まれます。公開Issueにそのまま掲載せず、個体識別部分を伏せてください。
キーボードの入力内容やボタン操作の全履歴は記録しません。ファイル書き込みは専用ワーカーがまとめて行います。

## 読み方

- diagnostic-session: 版、プロセス、OS、実行環境。途中でログをオンにしても一覧を再記録します。
- inventory: 台数だけでなく機器名、VID/PID、パス、SDL instanceを記録。VID/PIDだけで仮想機器とは断定しません。
- selection: 自動選択、指定パス一致、名前での代用、候補不在、接続済みのため選択省略を区別。
- open-begin / open-end: 試行番号、所要時間、成功・失敗、直後に取得したSDLエラー。
- connected / stage=handle_open: ハンドル取得のみ。Bluetooth認証成功や入力受信の証明ではありません。
- input-evidence: 接続後またはログ有効化後の最初の非ゼロボタンスナップショット。
  入力経路では数値のみを保持し、次の走査時に記録します。実際の観測時刻はobserved_monoを参照します。
  生HIDレポートや出力先での受信確認ではなく、入力元が仮想なら実機の証明にもなりません。
- SDL device event: SDL時刻と処理時点での経過時間。無線上の発生時刻ではありません。unknownは不明であって0msではありません。
- ui-pump: 画面側イベント取得の500ms超の間隔、50ms以上の処理時間。
- input-worker-summary: 5秒ごとの最大取得間隔・ロック待ち・SDL更新時間。
  再走査や未接続時の待機も含み、端末からゲームまでの遅延ではありません。
- bluetooth-probe: 探索開始／終了とWindowsの登録・認証・接続フラグ。探索自体は接続要求ではありません。
- virtual-output: 仮想機器の生成／破棄。inventoryとの時間的対応は手掛かりであり、同一機器の確証ではありません。
- log-health: キュー満杯による欠落、書き込み失敗、失われた行数の累積値。
  保存先が復旧した際に記録します。永久的な書き込み不能や強制終了時はログに残せない場合があります。

## 第2段階：比較試験

順番はアプリ完全終了 → Keyboard & Mouse → XInput → DirectInput。
3出力の比較は統合版で行い、実機、入力元設定、起動操作、距離、電池状態、他ソフトを揃えます。
固定出力版／公開版は共通診断の動作確認用です。方式切り替え後は一度アプリを終了して起動し直します。
アプリ終了条件ではWindows側の表示と別の入力確認手段を使います。他の入力確認ソフトは同時に複数起動しません。

成功は「対象HVCが復帰し、実際のボタン操作が届くこと」。仮想Xboxが一覧にあるだけなら成功ではありません。
Windows側でも消えるか、SDLでは見えるが開けないか、別の機器を選ぶか、開けるが入力がないかを分類します。

記録項目: 条件 / 試行番号 / 復帰操作時刻 / 再接続ボタン時刻 / 実機入力確認時刻 /
選択された機器 / 結果 / 再ペアリング有無 / 対応ログ。

## 自動検証の範囲

--diagnostic-self-test は時刻変換、選択理由、満杯キュー、書き込み例外、復旧、終了競合を検証します。
--self-test にも同じ検証を組み込みました。これらはBluetooth実機再接続を検証しません。
定常診断処理の割り当て検証は tests/DiagnosticsHarness、測定結果と限界は DIAGNOSTICS-VALIDATION.md を参照してください。
追加の依存ライブラリはありません。共通診断コードは各版に同一内容で配布します。
